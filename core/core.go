// Package core contains the core functionality of halp and initializes the default
// parser.
package core

const (
	// AppName is a constant for halp so that we can be consistent on the
	// naming across all uses of the name.
	AppName = "halp"
)
